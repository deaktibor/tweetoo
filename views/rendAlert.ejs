<!-- Alert render-->
<% if (typeof rendAlert !== 'undefined') { %>
    <div class="d-flex justify-content-center">
        <div class="alert <%= rendAlert.type %>" role="alert">
            <%= rendAlert.message%>
        </div>
    </div>
<% } %>
