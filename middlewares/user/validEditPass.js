/**
 * feldolgoza és validálja a bejovo paramétereket
 * @param objRepo
 * @returns {function(*, *, *): *}
 */
module.exports = function (objRepo){
	return (req, res, next) => {
		return next();
	}
}
