/**
 * kiküldi az adot email-re a jelszavat
 * @param objRepo
 * @returns {function(*, *, *): *}
 */
module.exports = function (objRepo){
	return (req, res, next) => {
		//Email service implementation for send pass
		return next();
	}
}
